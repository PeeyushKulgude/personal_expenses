import 'package:flutter/material.dart';

class SPDbAndController extends StatelessWidget {
  const SPDbAndController({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Divider()],
    );
  }
}
